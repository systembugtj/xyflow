# Vanilla Flow Example

这是一个使用 `@xyflow/vanilla` 包的简单示例。

## 运行示例

```bash
pnpm dev
```

示例将在 http://localhost:3001 打开。

## 功能

- 节点拖拽
- 视图平移和缩放
- 添加节点
- 适应视图
- 缩放控制
- 删除选中元素

## 代码说明

这个示例展示了如何使用 `@xyflow/vanilla` 创建一个基本的节点编辑器：

1. 导入 XYFlow 类和样式
2. 创建初始节点和边数据
3. 初始化 XYFlow 实例
4. 添加交互控制按钮
